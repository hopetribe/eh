# r16/r17 发布记录

状态：已部署。服务器启动时间：2026-09-05 16:36:40 CST。

- 用户授权：提交和部署。
- 目标代码：29c49f703117c9f79a93759e5d53718edc1b8a57（含已完成的r16/r17）。
- 服务器基线：ecbe666944c9f2fd252018a540e0e90a3358123c。
- 运行文件变更：`gcn/backtest/engine.py`、`historical_research.py`、`signal_research_r4.py`、`signal_research_r16.py`、`signal_research_r17.py`及`gcn/recipes/gcn_main.py`。
- 正式策略保持v5；r17候选已否决，不启用。固定失效价仅为显式开启的研究能力，配方仅在诊断模式扩展S来源列。
- r18尚只有未实现模块的红灯测试，保留本地，不提交或部署；11份本地行情缓存同样排除。
- 不更新前端、依赖或部署配置，保留已修复的信号列表；不初始化/更新真实shadow，不主动刷新行情。

## 验证与回滚

- 从29c49f7提交对象生成独立验证与发布源，不使用含r18测试的工作区。
- 独立快照323项研究/核心/金样/影子/Web/API回归通过（153.36秒），其中包括90组关闭固定失效价时与旧引擎的逐值等价检查。
- r16/r17共13份结果、18份冻结源码及协议/输入父链摘要通过；r17当前10份算法源码亦匹配。r16的旧引擎、旧历史评估冻结副本保留原样，不要求其摘要与r17修改后源码相同。
- 核心10股×4版本默认输出与r15冻结配方逐值完全一致。
- 发布包只含6份白名单源码，SHA-256：`6489bfdf1d1c72cc83af4ae42b005fa3f9979fe3354b9d64754ab8263c1f28e7`。
- 服务器预检65份运行文件逐项匹配ecbe666，2份新增文件原本不存在；服务active/running，真实shadow不存在。
- bc6e7b9及29c49f7已推送GitHub main。既有发布锁下备份、停止服务并同步6份白名单文件，服务器Python 3.12.3长期venv编译/导入通过，随后启动成功。
- 发布后67份运行文件摘要完全匹配29c49f7，服务active/running、NRestarts=0。
- 发布前公网可信TLS首页/样式摘要一致，已保存900根v5计算及完整回测JSON基线，HTTP入口308跳转正确。
- 内网与公网v5的900根计算及完整回测JSON分别与发布前完全相同；公网首页和样式摘要匹配，HTTPS可信TLS与HTTP 308跳转正常。
- 回滚备份：`/home/eric/kk2/.deploy-backups/29c49f7-release.jtx16c4z`，状态DEPLOYED，本次未触发回滚。
- 服务器确认r18模块未发布、真实shadow目录仍不存在。11份本地行情缓存及r18未完成测试的摘要与发布前完全相同；未刷新行情、修改依赖或服务配置。
- 发布记录与计划日志单独提交，不包含新运行代码，无需再次重启服务。

访问：[金筹九转 GCN](https://43.160.201.247:8443/)。
