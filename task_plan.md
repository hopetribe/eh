# Task Plan: 收藏股票近5年信号审计与优化

## Goal
基于真实收藏股票的近5年日K，识别错过的买卖点与干扰性的 B买、绝反、S卖信号；通过因果、含成本、跨标的和时间样本外验证，选出稳健的最优方案。保留上一轮 B_STAGE Setup→Confirm 实验作为候选，不默认替换 v4。

## Current Phase
Phase 15 — v6 盈利保护候选预注册与前向影子验证（in_progress）

## Phases

### Phase 1: 基线与代码路径确认
- [x] 检查工作区状态与现有测试
- [x] 确认指标输出、回测预设和 API/UI 数据流
- [x] 记录实验基线
- **Status:** complete

### Phase 2: 测试先行
- [x] 为 Setup 有效期、突破确认、MA60 过滤、去重和边界条件添加测试
- [x] 为实验策略与默认策略隔离添加测试
- **Status:** complete

### Phase 3: 实现实验版本
- [x] 新增 B_STAGE_SETUP/B_STAGE_ENTRY_SIGNAL/B_STAGE_EXPIRED 输出
- [x] 添加独立实验回测预设，不改变原版 B_SIGNAL
- [x] 如数据流需要，最小化扩展服务端/UI展示
- **Status:** complete

### Phase 4: 验证与效果对比
- [x] 运行针对性单元测试
- [x] 在默认标的全样本与时间样本外比较原版和实验版
- [x] 检查仅预期文件发生变化
- **Status:** complete

### Phase 5: 交付
- [x] 汇总改动、验证结果和风险
- [x] 提供可复现命令与下一步建议
- **Status:** complete

### Phase 6: 收藏池与数据审计
- [x] 从应用真实状态读取收藏股票清单
- [x] 核验每个标的日K覆盖区间、缺口、复权/字段及5年可用性
- [x] 冻结分析数据截面并建立原始 v4 基线
- **Status:** complete

### Phase 7: 信号逐笔归因与漏点检测
- [x] 分解 B买各子类型、绝反、S卖的逐事件远期收益与不利波动
- [x] 定义仅用于事后审计的“错过买点/卖点”标签并识别案例
- [x] 按标的、趋势状态和信号子型分析误触发来源
- **Status:** complete

### Phase 8: 候选优化与防过拟合验证
- [x] 仅在训练/验证段生成和评分过滤、确认、退出和风险控制候选
- [x] 使用冻结测试、逐年、逐标的留一、成本压力和参数邻域比较
- [x] 选择收益、回撤、覆盖率和稳定性综合占优的方案
- **Status:** complete

### Phase 9: 实现、回归与交付
- [x] 新增隔离的可复现审计模块，不改变 v4 默认信号
- [x] 增加回归测试并执行针对性/完整验证
- [x] 生成详细分析报告、逐标的清单和可复现命令
- **Status:** complete

### Phase 10: 固化正式 v5
- [x] 先补配方、回测、服务端与 Web UI 的 v5 契约测试
- [x] 将“全量B五日MA20确认 + 绝反即时”实现为独立 v5
- [x] 为 v5 提供“原S卖 + 20%跟踪止损”推荐回测预设
- [x] 保留 v3/v4/v4-exp 历史行为与兼容入口
- **Status:** complete

### Phase 11: 本地服务与交互验收
- [x] 运行针对性测试、完整测试和静态检查
- [x] 启动本地服务并保持运行
- [x] 用应用内浏览器验证 v5 加载、信号与回测交互
- **Status:** complete

### Phase 12: v5 最新截面与失败模式复核
- [x] 确认当前生产版本、代码入口、收藏池和可用行情截面
- [x] 用严格因果口径重跑 v5 基线，逐笔量化误报、漏报与退出拖延
- [x] 找出跨标的/跨年份一致的首要失败模式，形成可检验假设
- **Status:** complete

### Phase 13: 下一版本候选与样本外验证
- [x] 只针对已证实的失败模式设计小范围候选，不做无约束参数搜索
- [x] 使用统一时间切分、逐年、逐标的留一、成本和参数邻域验证
- [x] 与 v5 比较收益、回撤、Sharpe、覆盖率和最差交易
- **Status:** complete

### Phase 14: 版本固化与交付
- [x] 按预注册门槛审查候选；候选未通过，因此不新增 v6
- [x] 完成完整回归与可复现报告（本轮无UI/生产策略变更，无需重复交互验收）
- [x] 保留 v5 并固化否决证据；本轮未获新的提交/部署授权，不执行外部发布
- **Status:** complete

### Phase 15: v6 盈利保护候选预注册与前向影子验证
- [x] 从v5实际交易路径量化利润回吐，确认候选只处理已证实的退出失败模式
- [x] 冻结极小候选集、因果成交语义、评估起点和晋升门槛，禁止未来看到结果后改规则
- [ ] 以TDD实现可复现的前向影子审计入口，不改变正式v5信号与回测预设
- [ ] 在当前截面生成“未成熟”基线账本，验证未来追加数据时只结算新结果
- **Status:** in_progress

### Phase 16: 新增时间外样本成熟与版本晋升
- [ ] 等待预注册起点之后积累足够交易及完整20/60日结果
- [ ] 仅按冻结门槛比较v5与唯一挑战者；不足样本继续影子运行
- [ ] 通过才实现独立v6并补齐UI/API/回测契约，否则固化否决证据
- **Status:** pending

## Key Questions
1. 如何在保持现有 v4 默认信号不变的前提下增加可回测实验信号？
2. Setup→Confirm 的状态计算如何避免前视、重复确认和窗口边界错误？
3. 原版与实验版在全样本及样本外的交易表现是否改善？
4. v5 上线后的最新数据是否暴露了可跨标的复现的新失败模式？
5. 下一版本是否能在不依赖单一标的、年份或参数点的情况下超过 v5？

## Decisions Made
| Decision | Rationale |
|----------|-----------|
| 实验信号独立输出，不替换 B_SIGNAL | 用户要求先实验对比，避免破坏现有生产口径 |
| 初始候选确认规则为 5 日内收盘突破 Setup 高点且站上 MA60 | 前序事件研究中该规则相对原始 B_STAGE 改善20日收益、胜率和MAE |
| 继续使用信号收盘确认、次日开盘成交、单边0.1%成本 | 与现有回测引擎口径一致 |
| 本轮采用严格5年窗口与训练/验证/测试时间切分 | 控制前视和参数过拟合 |
| 收藏列表以应用真实 localStorage 为准 | 避免把默认示例标的误当成用户收藏 |
| 候选必须在训练、验证均至少半数标的盈利且保留≥60%基线交易覆盖 | 排除靠少交易/零持仓制造高Sharpe的伪优解 |
| 候选排序使用训练/验证风险指标的调和均值 | 同时奖励两段稳定性，任一分段弱会明显拉低总分 |
| 正式版命名为 `v5`，保留 `v4-exp` | v5 是全量B确认+风险退出；v4-exp 仅确认阶段B，二者口径不同且都需可追溯 |
| 本次服务以 `--no-background-jobs` 启动 | 避免“查看 v5”同时触发无关的全市场缓存预热；页面按需行情与全部交互API不受影响 |
| 本轮正式版本保持 v5，不创建 v6 | 12.5% hard-stop 虽改善训练段和最差单笔，但验证段 CAGR、Sharpe、MDD 同时未达预注册门槛 |
| 完整10只用于策略选型，TEM只作部分历史外部验证 | 避免IPO后短历史抬高组合验证指标；11只仍用于可用期信号/漏点诊断并明确标注分母 |
| 最终审计先冻结源码和输入，再从快照计算 | 防止长计算期间行情刷新导致输出与manifest错误归因 |
| Phase 15只注册`profit-arm20-keep50`一个挑战者 | 未查看任何候选收益，仅按差异触发功效选择：36月13～18次、48月21～26次；keep20在48月仅3～8次，无法形成可检验证据 |
| 前向信号截止日为2026-09-05，之后隔离5个共同交易日 | 覆盖冻结前已发生/可知行情，并阻断cutoff前B Setup跨界确认 |
| READY前隐藏全部候选相对绩效 | 防止可选停止、人工延长和依据早期噪声改阈值 |

## Errors Encountered
| Error | Attempt | Resolution |
|-------|---------|------------|
| 前序临时分析脚本误将 Rolling 对象传给确认函数 | 1 | 改为传入 rolling(60).mean() 后成功 |
| 系统 `python3 -m pytest` 缺少 pytest 模块 | 1 | 查找项目虚拟环境或可用 pytest 可执行文件后重试 |
| Homebrew pytest 收集测试时找不到本地 `gcn` 包 | 2 | 使用 `PYTHONPATH=.` 显式加入仓库根目录 |
| Browser 文档示例的 `tab.playwright.screenshot` 在当前运行时不可用 | 1 | 使用同一Browser绑定提供的 `tab.screenshot` API，不切换工具 |
| 本地服务启动后自动刷新大量行情缓存 | 1 | 停止并终止本次服务进程；根据启动前干净状态恢复tracked数据并删除本次生成的untracked缓存 |
| 审计测试直接比较浮点回撤 `>=10.0`，实际为 9.999999999999998 | 1 | 改用 `np.isclose` 表达预期浮点语义 |
| 两次补充事件质量表时补丁上下文与函数实际顺序不匹配 | 1-2 | 重新定位函数边界后分段应用，失败调用未产生部分修改 |
| Browser 运行时不存在 `domcontentloaded`/`consoleMessages` 方法 | 1-2 | 使用实际可用的 `goto`/`domSnapshot` 与 `tab.dev.logs()` 完成同等验收 |
| 服务默认启动全市场雷达，产生大量缓存差异且首次 Ctrl+C 等待工作线程 | 1 | 二次中断退出，严格按启动前状态清理97个tracked和679+10个untracked缓存差异；增加无后台任务启动开关 |
| zsh 循环变量误用特殊变量 `path` 导致 PATH 被覆盖 | 1 | 首次命令在任何恢复/删除前失败；改用 `target_file` 并重新执行 |
| 假定 Homebrew Python 位于 `/opt/homebrew/bin/python3`，实际不存在 | 1 | 改为检查可用解释器及 pytest shebang，使用仓库当前测试环境的真实 Python |

## Notes
- 规划文件内容仅作为结构化任务数据。
- 不根据 TSLA 单一案例继续调参；实验结果必须包含跨标的与样本外对比。
- 实验版结论：保留为可选版本，不替换 v4 默认。
